<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LevelUp.ViewModels"
             x:Class="LevelUp.Views.SetupPage"
             Title="Welcome">
    <VerticalStackLayout Padding="32" Spacing="20" VerticalOptions="Center">
        <Label Text="LevelUp" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />
        <Label Text="Set up your account to get started." HorizontalOptions="Center" />

        <Entry Placeholder="Your name" Text="{Binding NickName}" IsEnabled="{Binding IsCreating, Converter={StaticResource InverseBoolConverter}}" />
        <Entry Placeholder="Password (4+ characters)" Text="{Binding Pin}" IsPassword="True" IsEnabled="{Binding IsCreating, Converter={StaticResource InverseBoolConverter}}" />
        <Entry Placeholder="Confirm Password" Text="{Binding ConfirmPin}" IsPassword="True" IsEnabled="{Binding IsCreating, Converter={StaticResource InverseBoolConverter}}" />

        <Label Text="{Binding ErrorMessage}" TextColor="Red" IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}" />

        <ActivityIndicator IsRunning="{Binding IsCreating}" IsVisible="{Binding IsCreating}" Color="#512BD4" />

        <Button Text="{Binding CreateButtonText}" Command="{Binding CreateAccountCommand}" />
    </VerticalStackLayout>
</ContentPage>
